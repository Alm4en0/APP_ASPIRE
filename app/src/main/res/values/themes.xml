<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.Prototipoproyecto" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here -->
    </style>

    <style name="Theme.Prototipoproyecto" parent="Base.Theme.Prototipoproyecto">
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="android:colorPrimary">@color/header</item>
    </style>

    <style name="MyActionBar" parent="Widget.Material3.ActionBar.Solid">
        <item name="background">@color/header</item>
        <item name="titleTextColor">@android:color/white</item>
    </style>
</resources>